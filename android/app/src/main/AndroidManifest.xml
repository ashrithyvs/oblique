<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" tools:targetApi="33" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" tools:ignore="QueryAllPackagesPermission" />
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:name=".ObliqueApp"
        android:allowBackup="true"
        android:supportsRtl="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.Oblique">

        <activity
            android:name=".SplashActivity"
            android:theme="@style/Theme.Oblique.Splash"
            android:exported="true">   <!-- required for launcher on Android 12+ -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity
            android:name=".DashboardActivity"
            android:theme="@style/Theme.Oblique.NoEdgeToEdge" />

        <activity
            android:name=".PromptActivity"
            android:theme="@style/Theme.Oblique.Dialog" />

        <activity android:name=".WelcomeActivity" />
        <activity android:name=".PermissionsActivity" />
        <activity android:name=".PinSetupActivity" />
        <activity android:name=".PinConfirmActivity" />
        <activity android:name=".PinUnlockActivity" />
        <activity android:name=".AppListActivity" />

        <service
            android:name=".MonitoringService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <service
            android:name=".OverlayService"
            android:exported="false" />
    </application>
</manifest>
